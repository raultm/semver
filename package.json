{
  "devDependencies": {
    "gulp": "^3.6.2",
    "exec-sync": "^0.1.6",
    "prompt": "^0.2.13",
    "mocha": "^1.20.0",
    "sync-exec": "^0.3.2"
  },
    "scripts": {
        "test": "mocha -R spec"
    }
}
